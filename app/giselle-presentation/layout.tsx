import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Bank Trax: Depositos No Recibidos',
  description: 'Customer Support Training — Ninja Guide for bank transaction deposit tags.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
