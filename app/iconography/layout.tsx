import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Iconography',
  description: 'Félix icon system — Phosphor icon library usage, sizing guidelines, and icon patterns for product interfaces.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
